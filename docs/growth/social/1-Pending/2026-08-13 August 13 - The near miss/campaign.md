# August 13: The near miss

Video cut 9, curiosity gap (second pass), run `20260730-0613`. Static:
Facebook link 3 (Chicago), Instagram feed 3 (first and absent), and Threads'
existing post 3 (the near miss, chained across 2 posts).

**Channels:** facebook (link, reel), instagram (feed, reel), threads (feed),
tiktok (video), youtube (shorts), linkedin (feed), gbp (post), x (feed).

**Destination:** mostly the bilingual research pages (Berlin, Madrid, Paris)
plus `getbrandgeo.com/ai-visibility-for-chicago.html`, `/bg-004.html` and
`/bg-034.html` and `/bg-027.html` for the new slots, plus the bio link for
Instagram, Threads and TikTok (no in-caption link on any of the three).

**UTM campaign:** `campaign2607` on most tagged links that predate this pass;
the Facebook link card uses `utm_campaign=chicago` instead. `pulse` on the
new linkedin, gbp and x links added in this pass.

Per `docs/growth/PUBLISHING-PLAN.md`, week 2, Thu 08-13. Note: the plan names
Cut 8 for this day; the video files actually on disk here are Cut 9, one
weekday's worth of drift, see the session report for detail.

---

## Backfill pass, 2026-08-07

Added the three slots this day was missing against the brief's gap table.
`facebook/feed` proper was left out by the brief's own instruction, since
`facebook/link` already covers a feed-adjacent format for this day, and time
went to slots this day had none of at all instead.

- **linkedin/feed**: fresh single post, the "Only You Boutique Hotel" versus
  "Only YOU Boutique Hotel Madrid" naming inconsistency from `bg-034.html`.
  Chosen for this day specifically: a brand name that reads as two different
  strings to a pattern matcher is exactly a near miss, the theme this folder
  is already named for, distinct from the invented-firm-name near miss the
  existing Threads post here already covers.
- **gbp/post**: `gbp-7`, the Managed plan (from EUR 1,500/mo, all seven
  engines, 200 prompts, done for you). Next unclaimed number in the evergreen
  GBP series; Managed had no GBP post of its own yet.
- **x/feed**: a fresh 4-tweet thread, the Madrid airport hotel exception from
  `bg-027.html`, the one category in the whole four city study where the two
  languages converged, because it is dominated by international chains whose
  names do not change across languages. Distinct from the Paris, Rome and
  Berlin examples already used earlier in the week. Character counts verified
  programmatically; all four tweets clear 280 characters with margin.

Nothing pre-existing in this folder was edited.

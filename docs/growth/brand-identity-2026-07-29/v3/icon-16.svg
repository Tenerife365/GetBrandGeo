<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" role="img" aria-label="BrandGEO">
  <title>BrandGEO</title>
  <desc>Small-size variant, for 16px and 24px only. The dot is omitted: at 16px
  the gap around it computes to 0.73 device px, so it fuses into the ring and the
  bowl turns into a solid blob. The annulus alone still reads as a b at a size
  where no dot could be resolved anyway. Everything else is identical.</desc>
  <defs>
    <linearGradient id="tileGrad16" gradientUnits="userSpaceOnUse"
                    x1="0" y1="0" x2="512" y2="512">
      <stop offset="0"    stop-color="#6366F1"/>
      <stop offset="0.55" stop-color="#8B5CF6"/>
      <stop offset="1"    stop-color="#7C3AED"/>
    </linearGradient>
  </defs>

  <rect width="512" height="512" rx="120" fill="url(#tileGrad16)"/>

  <g transform="translate(43.5 43.5) scale(0.83)"
     fill="none" stroke="#F5F3FF" stroke-width="56" stroke-linecap="round">
    <path d="M156 116V388"/>
    <circle cx="272" cy="304" r="84"/>
  </g>
</svg>
